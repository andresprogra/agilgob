<%= form_with(model: shelf, local: true) do |form| %>
  <% if shelf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shelf.errors.count, "error") %> prohibited this shelf from being saved:</h2>

      <ul>
      <% shelf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pasillo %>
    <%= form.text_field :pasillo, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :levels %>
    <%= form.text_field :levels, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "Save", class: "btn btn-xs btn-success" %>
  </div>
<% end %>
